function validationExistence(input, dates){
    if (!dates.includes(input)){
    } 
    return true
};

function validationInput(input){
    if (input || !typeof input === "string"){
    }
    return true
}

